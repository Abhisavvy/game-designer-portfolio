"use client";

/**
 * Production stub for client error boundaries under `src/app/admin/` (`error.tsx`, …).
 */
export default function AdminErrorOmitted({
  error: _error,
  reset: _reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return null;
}
