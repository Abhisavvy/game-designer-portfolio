import { notFound } from "next/navigation";

/**
 * Production stub for `page` and `default` modules under `src/app/admin/` (parallel routes).
 */
export default function AdminPageOmitted() {
  notFound();
}
