/**
 * Production stub for loading modules under `src/app/admin/` (`loading.tsx`, …).
 * Keeps a valid React Server Component boundary without rendering admin UI.
 */
export default function AdminLoadingOmitted() {
  return null;
}
